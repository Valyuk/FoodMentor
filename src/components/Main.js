import { Box } from "@mui/material";
import { styled } from "@mui/system";

const Main = styled(Box)({
  display: "flex",
  flexDirection: "column",
  textAlign: "center",
  alignItems: "center",
  height: "812px",
});

export default Main;
